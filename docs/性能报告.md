# 性能与优化报告

## 计时字段说明

每一步都会在 `summary.json` 中记录耗时：

- `field_build_s`
- `axis_s`
- `psi_fit_s`
- `surface_screen_s`
- `surface_screen_curve_newton_s`
- `surface_screen_fieldline_trace_s`
- `surface_extract_1d_newton_s`
- `boozer_ls_s`
- `boozer_newton_s`
- `boozer_qs_s`
- `boozer_candidates_s`

另外 `timing_b_calls` 会按阶段统计我们自己控制的批量 `.B()` 调用：

```text
B_time_s
B_calls
B_points
```

注意：Boozer LS/Newton 内部的 `.B()` 调用由 simsopt 控制，当前不拆进 `timing_b_calls`，只记录 LS/Newton 总时间。

## 当前示例实测

以下结果来自 `runs/01_raw_verified` 和 `runs/debug_raw_verified`。两个例子都使用：

```text
poly_degree = 10
m_tor = 12
psi grid = 50 x 50 x 50
axis rk4_steps = 800
screen trace_steps = 600
surface_order = 6
OMP_NUM_THREADS = 1
```

### 总阶段耗时

| case | 找磁轴 | 拟合 $\psi$ | 找目标 $\psi_0$/cheap screen | Boozer 候选总耗时 | total |
|---|---:|---:|---:|---:|---:|
| `01/raw` | 72.22 s | 13.97 s | 7.10 s | 1.98 s | 95 s 量级 |
| `debug/raw` | 104.02 s | 14.22 s | 10.78 s | 2.21 s | 130 s 量级 |

这里“找目标 $\psi_0$”不是跑完整 Boozer，而是对候选 $\psi_0$ 做便宜筛选：截面曲线一维 Newton 提取 + 一周期场线追踪 + $\psi$ drift 判断。

### `.B()` 批量计算耗时

| case | 阶段 | `.B()` 时间 | `.B()` 次数 | 点数 |
|---|---|---:|---:|---:|
| `01/raw` | GA 找磁轴 | 70.45 s | 58112 | 14,746,112 |
| `01/raw` | $\psi$ fit 采样 | 0.47 s | 6 | 94,600 |
| `01/raw` | $\psi_0$ 筛选场线 | 6.08 s | 9600 | 1,228,800 |
| `debug/raw` | GA 找磁轴 | 101.80 s | 64644 | 16,384,644 |
| `debug/raw` | $\psi$ fit 采样 | 0.58 s | 6 | 94,000 |
| `debug/raw` | $\psi_0$ 筛选场线 | 9.63 s | 12000 | 1,536,000 |

结论很清楚：GA 找磁轴目前几乎完全受 `.B()` 批量计算支配。$\psi$ fit 虽然也要算 `.B()`，但 `.B()` 本身只占 `0.5 s` 左右，主要耗时在设计矩阵、正规方程、线性求解和条件数计算。

### $\psi_0$ 筛选细节

| case | 候选数 | 截面曲线一维 Newton 总时间 | 磁力线追踪总时间 |
|---|---:|---:|---:|
| `01/raw` | 4 | 0.65 s | 6.31 s |
| `debug/raw` | 5 | 0.68 s | 9.94 s |

逐 level 的便宜筛选结果：

| case | $\psi_0$ | 一维 Newton | 一周期追踪 | $d_\psi$ p95 |
|---|---:|---:|---:|---:|
| `01/raw` | 0.02 | 0.159 s | 1.576 s | 8.96e-06 |
| `01/raw` | 0.04 | 0.172 s | 1.578 s | 1.66e-05 |
| `01/raw` | 0.08 | 0.159 s | 1.584 s | 3.04e-05 |
| `01/raw` | 0.12 | 0.163 s | 1.574 s | 4.26e-05 |
| `debug/raw` | 0.001 | 0.125 s | 2.000 s | 6.86e-06 |
| `debug/raw` | 0.002 | 0.113 s | 1.997 s | 9.93e-06 |
| `debug/raw` | 0.004 | 0.115 s | 1.972 s | 1.45e-05 |
| `debug/raw` | 0.008 | 0.160 s | 1.980 s | 2.12e-05 |
| `debug/raw` | 0.012 | 0.162 s | 1.987 s | 2.65e-05 |

### 最终候选面细节

| case | 最终 $\psi_0$ | 等值面一维 Newton | surface fit | Boozer LS | Boozer Newton | QS |
|---|---:|---:|---:|---:|---:|---:|
| `01/raw` | 0.12 | 0.914 s | 0.021 s | 0.103 s | 0.013 s | 0.018 s |
| `debug/raw` | 0.012 | 0.938 s | 0.021 s | 0.276 s | 0.012 s | 0.021 s |

对应结果：

| case | axis residual | best $\psi_0$ | $\iota$ | volume | $G$ | QP QS $(0,1)$ |
|---|---:|---:|---:|---:|---:|---:|
| `01/raw` | 8.60e-10 | 0.12 | -2.907716 | 6.157e-03 | 7.631125 | 2.464e-03 |
| `debug/raw` | 2.22e-09 | 0.012 | -2.196752 | 8.650e-05 | 8.060917 | 2.937e-06 |

注意：当前封装正确支持多根基础线圈。`debug/raw` 示例有 2 根基础线圈，经过 `nfp=5` 和 stellarator symmetry 后是 20 根总线圈；这和早期若干研究脚本只取第一根基础线圈的结果不完全可比。

## OpenMP 线程设置

当前默认：

```text
OMP_NUM_THREADS=1
OPENBLAS_NUM_THREADS=1
MKL_NUM_THREADS=1
NUMEXPR_NUM_THREADS=1
```

原因是 simsopt 的 `.B()` 在批次点数较少时，如果 `OMP_NUM_THREADS>1`，线程同步开销可能远大于实际磁场计算。GA 轴搜索和候选面筛选虽然是批量点，但单次批次规模通常只有几百点，所以默认单线程更稳。

若以后把批次扩大到数万点，或改成 GPU 后端，可以重新测试多线程策略。

## 适合 GPU 优化的步骤

### 1. GA 磁轴搜索

GA 每代有 256 条场线，每条固定步长 RK4。不同点之间完全独立，适合 GPU：

$$
(R_i,Z_i)\rightarrow(R_i',Z_i'),\qquad i=1,\ldots,256.
$$

如果后续把种群扩大到几千或几万点，GPU 收益会更明显。

### 2. $\psi$ 最小二乘采样

$B\cdot\nabla\psi=0$ 的设计矩阵按采样点逐行构造。每个采样点之间独立，适合 GPU 批量计算：

$$
A_{ij}=B_i\cdot\nabla f_j(x_i,z_i,\Phi_i).
$$

不过最终正规方程或 QR/SVD 解仍需要注意数值稳定性。

### 3. 候选 $\psi_0$ drift 筛选

每个候选 level 上有 `n_alpha` 条短场线，只追踪一个 field period。它和 GA 类似，适合批量化或 GPU 化。

## 暂不优先 GPU 化的步骤

### Boozer LS/Newton

Boozer solve 依赖 simsopt 的 surface、残差、导数和优化器。它的单次规模不大，但控制流和依赖复杂。第一阶段建议先把初值筛选做好，减少 Boozer 调用次数，而不是先改 Boozer 内核。

## 当前第一版策略

为了避免反复跑完整测试，第一版使用：

1. 便宜的 $\psi$ drift 筛选多个 $\psi_0$。
2. 只对最大的几个通过筛选的 level 跑 Boozer。
3. 一旦较大的候选 Newton 成功，就停止继续尝试更小 level。

这样可以把昂贵步骤集中在最有可能成为“最大磁面”的候选上。
